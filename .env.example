PGUSER=otflow
PGPASSWORD=change-me
PGHOST=localhost
PGPORT=5432
PGDATABASE=otflow

# Or set a single connection string instead of the PG* vars above:
# DATABASE_URL=postgresql://otflow:password@localhost:5432/otflow

JWT_SECRET=change-this-to-a-long-random-string
PORT=4000
SEED_SUPERADMIN_PASSWORD=admin123
